import styled from 'styled-components';
import MaterialIcon from 'react-native-vector-icons/MaterialIcons';

export const ArrowLeft = styled(MaterialIcon)`
  font-family: material;
  font-size: 20px;
  color: #ffffff;
`;
